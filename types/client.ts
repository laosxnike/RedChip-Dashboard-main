// galileo-design/types/client.ts

// If `client.ts` is no longer needed for client-specific components, you can remove or repurpose it.
// Alternatively, ensure it only contains types that are not duplicated in `clientDashboard.ts`.

export interface ChartData {
  name: string;
  value: number;
}
  